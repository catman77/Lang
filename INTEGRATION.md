# Space Language: Интегрированная система анализа

Полная реализация многоуровневой системы анализа дискретных и непрерывных структур.

## 🎯 Обзор

Space Language объединяет **9 компонентов** в единый пайплайн, преобразующий дискретные правила переписывания в богатые математические структуры.

### Архитектура (8 уровней)

```
L0: Дискретная система переписывания
    ↓
L1: Машина Тьюринга
    ↓
L2: Графы G_L и SCC анализ
    ↓
L3: Перекрытия (AC-автоматы)
    ↓
L4: Семантические подъёмы (макросы)
    ↓
L5: Генерация текста (AST)
    ↓
L6: Верификация (Z3, Isabelle)
    ↓
L7: Математические подъёмы
    • Геометрия (многообразия, метрики)
    • Меры (энтропия, инвариантность)
    • Категории (функторы, морфизмы)
```

## 📦 Компоненты

### 1. Система переписывания (L0)
**Файлы**: `src/rewriting/`  
**Строк кода**: 325

Ядро системы с операциями:
- `bounded_reach`: Достижимые строки с ограничением
- `reachable`: Все достижимые строки
- `omega_limit`: ω-предел динамики
- `normal_forms`: Нормальные формы

```python
from rewriting import String, Rule, RewritingEngine

rules = [Rule(String.from_str("00"), String.from_str("|"))]
engine = RewritingEngine(rules)
reachable = engine.bounded_reach(String.from_str("0000"), max_steps=10)
```

### 2. Машина Тьюринга (L1)
**Файлы**: `src/turing/`  
**Строк кода**: 405

Симулятор МТ с BFS поиском:
- Парсинг определений МТ
- Пошаговое выполнение
- Поиск принимающих состояний

```python
from turing import parse_tm, TMSimulator

tm = parse_tm("tm_definition.txt")
simulator = TMSimulator(tm)
result = simulator.run("input_string", max_steps=100)
```

### 3. Графы G_L (L2)
**Файлы**: `src/graph/`  
**Строк кода**: 407

Построение графа достижимости и SCC:
- Граф G_L с параметром локальности L
- Алгоритм Тарьяна для SCC
- Анализ аттракторов

```python
from graph import GraphBuilder, tarjan_scc

builder = GraphBuilder(rules)
graph = builder.build(initial_string, depth=5)
sccs = tarjan_scc(graph)
```

### 4. AC-автоматы (L3)
**Файлы**: `src/overlap/`  
**Строк кода**: 370

Поиск перекрытий и критических пар:
- Aho-Corasick автомат
- Обнаружение перекрытий правил
- Критические пары для конфлюентности

```python
from overlap import AhoCorasick

ac = AhoCorasick()
for rule in rules:
    ac.add_pattern(rule.left)
ac.build()
matches = ac.search(string)
```

### 5. Макро-система (L4)
**Файлы**: `src/semantic_lift/`  
**Строк кода**: 480

Семантические подъёмы:
- Частотный анализ паттернов
- Проверка локальной конфлюентности
- Bounded bisimulation
- Создание словаря макросов

```python
from semantic_lift import FrequencyAnalyzer, Macro

analyzer = FrequencyAnalyzer()
analyzer.add_string(string)
macros = analyzer.get_frequent_substrings(min_frequency=2)
```

### 6. Генерация текста (L5)
**Файлы**: `src/text_generation/`  
**Строк кода**: 910

AST и линеаризация:
- Узлы AST (Token, Word, Phrase, Sentence)
- Translator с фактами
- Linearizer с стратегиями кодирования

```python
from text_generation import ASTNode, Translator

node = ASTNode(type="sentence", value="Hello", children=[])
translator = Translator()
text = translator.translate(node)
```

### 7. Верификация (L6)
**Файлы**: `src/verification/`, `src/isabelle/`  
**Строк кода**: 492 (Z3) + 504 (Isabelle) = 996

Формальная верификация:
- Z3 SMT-решатель для свойств
- Isabelle/HOL генератор теорем
- Docker интеграция для Isabelle2025-1

```python
from verification import Z3Checker
from isabelle import IsabelleTheoryGenerator, IsabelleDockerRunner

# Z3
checker = Z3Checker(rules)
result = checker.check_property(PropertyType.TERMINATION)

# Isabelle
generator = IsabelleTheoryGenerator(rules)
generator.generate_all()
runner = IsabelleDockerRunner()
runner.build_theory("formal/SpaceLanguageBasic.thy")
```

### 8. Математические подъёмы (L7)
**Файлы**: `src/math_lifts/`  
**Строк кода**: 1438 (geometry: 498, measure: 476, category: 464)

Три компонента непрерывной математики:

#### 8.1 Геометрия
Погружение в ℝⁿ:
- Метрические пространства (расстояние Левенштейна)
- Многообразия с размерностью и кривизной
- Геодезические линии
- Топологический анализ

```python
from math_lifts.geometry import GeometricLift, MetricSpace, Manifold

lift = GeometricLift(embedding_dim=10)
point = lift.embed_string(string)  # String → Point в ℝ¹⁰

metric = MetricSpace()
distance = metric.distance(s1, s2)  # Левенштейн

manifold = Manifold(points, metric)
dimension = manifold.estimate_dimension()
```

#### 8.2 Меры
Вероятностные меры и энтропия:
- Меры на пространстве строк
- Вероятностные меры с нормировкой
- Энтропия Шеннона
- Инвариантные меры под переписыванием
- Эргодический анализ

```python
from math_lifts.measure import Probability, FrequencyMeasure, InvariantMeasure

# Частотная мера
freq = FrequencyMeasure()
freq.observe_many(strings)
prob = freq.get_probability()
entropy = prob.entropy()

# Инвариантная мера
inv = InvariantMeasure(rules)
inv_measure = inv.find_invariant(strings)
```

#### 8.3 Категории
Категорная абстракция:
- Объекты и морфизмы
- Композиция стрелок
- Функторы между категориями
- Естественные преобразования
- RewritingCategory
- GeometricFunctor

```python
from math_lifts.category import RewritingCategory, GeometricFunctor

cat = RewritingCategory(rules)
cat.add_rewriting_step(source, target, rule)

functor = GeometricFunctor(rewriting_cat, geometry_cat, lift)
F_obj = functor.map_object(obj)
```

### 9. Интеграция (NEW!)
**Файлы**: `src/integration/`  
**Строк кода**: 704

Единый оркестратор всех компонентов:

```python
from integration import SpaceLanguageOrchestrator, PipelineConfig

# Конфигурация
config = PipelineConfig(
    graph_depth=5,
    enable_z3=True,
    enable_geometry=True,
    enable_measure=True,
    enable_category=True,
    embedding_dim=10
)

# Оркестратор
orchestrator = SpaceLanguageOrchestrator(rules, config)

# Полный анализ
result = orchestrator.analyze(initial_string, verbose=True)

# Результаты всех уровней
print(result.summary())
result.save_json("results.json")
```

## 🚀 Быстрый старт

### Установка

```bash
# Клонировать репозиторий
cd Lang

# Установить зависимости
pip install z3-solver  # Для верификации
```

### Примеры

```bash
# Базовая система переписывания
python examples/arithmetic_demo.py

# Полный пайплайн с макросами
python examples/full_pipeline_demo.py

# Интеграция всех компонентов
python examples/integration_demo.py

# Математические подъёмы
python examples/math_lifts_demo.py

# Верификация
python examples/verification_demo.py

# Isabelle формализация (требует Docker)
python examples/isabelle_demo.py
```

## 📊 Статистика

| Компонент | Строк кода | Статус |
|-----------|------------|--------|
| Rewriting | 325 | ✅ |
| Turing | 405 | ✅ |
| Graph | 407 | ✅ |
| AC-automata | 370 | ✅ |
| Macros | 480 | ✅ |
| Text generation | 910 | ✅ |
| Z3 verification | 492 | ✅ |
| Isabelle | 504 | ✅ |
| Math lifts | 1438 | ✅ |
| Integration | 704 | ✅ |
| **ИТОГО** | **6035** | **10/12** |

## 🎓 Математические основы

### Дискретный уровень
- **Теория переписывания**: конфлюентность, терминация, нормальные формы
- **Теория графов**: достижимость, SCC, аттракторы
- **Теория автоматов**: AC-автоматы, поиск паттернов

### Непрерывный уровень
- **Дифференциальная геометрия**: многообразия, метрики, кривизна
- **Теория меры**: вероятностные меры, энтропия, эргодичность
- **Теория категорий**: функторы, естественные преобразования

## 🔬 Исследовательские возможности

1. **Динамика систем**: изучение ω-пределов и аттракторов
2. **Конфлюентность**: проверка локальной/глобальной конфлюентности
3. **Семантические подъёмы**: автоматическое извлечение макросов
4. **Геометрические свойства**: размерность пространства строк
5. **Эргодичность**: временные vs пространственные средние
6. **Категорная абстракция**: функторы между уровнями

## 📝 Конфигурация

Класс `PipelineConfig` позволяет настроить анализ:

```python
config = PipelineConfig(
    # Граф
    graph_depth=5,              # Глубина построения G_L
    max_graph_size=100,         # Максимум вершин
    
    # Перекрытия
    min_overlap_length=1,       # Минимальная длина перекрытия
    
    # Макросы
    min_macro_frequency=2,      # Минимальная частота паттерна
    max_macro_length=10,        # Максимальная длина макроса
    
    # Верификация
    enable_z3=True,            # Включить Z3
    enable_isabelle=False,      # Включить Isabelle (требует Docker)
    z3_timeout=5000,           # Таймаут Z3 (мс)
    
    # Математика
    enable_geometry=True,       # Геометрические подъёмы
    enable_measure=True,        # Теоретико-мерные подъёмы
    enable_category=True,       # Категорные структуры
    embedding_dim=10,          # Размерность погружения
    
    # Текст
    enable_text_generation=True # Генерация текста
)
```

## 🐳 Docker (Isabelle)

Для работы с Isabelle формализацией:

```bash
# Генерация теорий
python -c "
from isabelle import IsabelleTheoryGenerator
gen = IsabelleTheoryGenerator(rules)
gen.generate_all()
"

# Сборка в Docker
docker run -v $(pwd)/formal:/workspace \
  makarius/isabelle:Isabelle2025-1 \
  isabelle build -D /workspace
```

## 📖 Результаты анализа

Класс `AnalysisResult` содержит:

- **L0**: `reachable_strings`, `omega_limit`, `normal_forms`
- **L2**: `graph_vertices`, `graph_edges`, `scc_count`
- **L3**: `overlap_pairs`, `critical_pairs`
- **L4**: `macros`, `macro_coverage`
- **L5**: `generated_text`, `ast_size`
- **L6**: `z3_results`, `isabelle_theories`
- **L7**: `geometry_data`, `measure_data`, `category_data`

Методы:
- `summary()`: краткое резюме
- `to_dict()`: полная сериализация
- `save_json(path)`: сохранение в JSON

## 🎯 Следующие шаги

- [ ] Docker CI/CD (автотесты, контейнеризация)
- [ ] Расширенная документация (туториалы, cookbook)
- [ ] Визуализация (графы, многообразия, меры)
- [ ] Оптимизация производительности
- [ ] Расширение библиотеки примеров

## 📄 Лицензия

MIT License

## 👥 Авторы

Space Language Research Team

---

**Версия**: 1.0  
**Дата**: Декабрь 2025  
**Статус**: 10/12 компонентов реализовано ✅
